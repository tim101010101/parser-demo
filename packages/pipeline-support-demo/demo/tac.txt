// a = 1 + 2 * (3 - 4)

t1 = 3 - 4        
t2 = 2 * t1       
t3 = 1 + t2       
a = t3            





for (let i = 0; i < 10; i++) {
  // ...
}

i = 0              // 初始化：将 0 赋值给 i
T1:                // 标签 T1，用于条件判断
if i < 10 goto T2  // 如果 i 小于 10，跳转到 T2
goto T3            // 否则跳转到 T3
T2:                // 标签 T2，循环体开始
// ...             // 循环体
i = i + 1          // i 自增 1
goto T1            // 跳转回 T1，进行下一次循环
T3:                // 标签 T3，循环结束