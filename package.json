{
  "name": "tiny-rollup",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "dev:parser": "turbo dev --filter='pipeline-support-demo'",
    "test:parser": "turbo test --filter='pipeline-support-demo'",
    "test:rollup": "turbo test --filter='rollup'"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
